math.sqrt<-function(x)
{
  return(sqrt(x))
}

math.log<-function(x)
{
  return(log(x))
}

math.log10<-function(x)
{
  return(log10(x))
}

math.log<-function(x)
{
  return(log2(x))
}

math.exp<-function(x)
{
  return(exp(x))
}


